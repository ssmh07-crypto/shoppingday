# 프로젝트 배경과 핵심 목표

이 문서는 Shoppingday의 제품 방향을 결정하는 기준 문서다. 기능을 설계하거나 구현할
때는 기술적 편의보다 이 문서의 운영 목표를 먼저 고려한다.

## 배경

사용자는 과거 스마트스토어에 약 10,000~20,000개의 상품을 등록하던 대량등록
판매자였다. 당시에는 최대한 많은 상품을 빠르게 등록하고, 그중 일부에서 판매가
발생하도록 만드는 전략으로 전체 매출을 확보할 수 있었다. 따라서 개별 상품의
상품명·키워드·태그를 정교하게 최적화하는 일의 상대적 중요도는 낮았다.

스마트스토어 정책 변경으로 일정 매출 기준을 충족하지 못하면 등록 가능한 상품 수가
약 1,000개로 제한된다. 이제는 상품 수를 무한히 늘리는 전략보다 제한된 슬롯을
효율적으로 사용하고, 판매 가능성이 확인된 상품의 성과를 높이는 전략이 중요하다.

## 핵심 목표

### 2026-09-08 사용자 확정 최종 범위

아래 최종 범위를 신규 개발·개선·기능 정리의 우선 기준으로 삼는다. 이 절은 목표이며
각 기능의 구현 완료를 의미하지 않는다.

1. 위탁 공급처에서 상품명·공급가·이미지·상세페이지·옵션·판매 상태를 가져온다.
2. 위탁상품은 아이템스카우트 엑셀의 실제 키워드·검색량을 활용해 상품명·태그·카테고리를
   빠르게 가공한다. 상품군별 재사용과 선택 상품 일괄 처리를 우선한다.
3. 가공·검증한 위탁상품은 위탁상품관리에서 스마트스토어에 등록한다.
4. 위탁판매의 핵심은 짧은 시간에 많은 상품을 적정 품질로 등록하는 것이다.
5. 등록 후 공급처의 품절·공급가·상세페이지 변경·단종을 추적하고 등록 상품에 반영한다.
   공급처 원본과 판매자가 가공한 상품명·태그·카테고리는 구분해서 보존한다.
6. 공급처 변경 확인과 반영은 예약 또는 수동 버튼으로 실행할 수 있게 한다. API 공급처와
   로그인된 브라우저가 필요한 공급처의 실행 가능 조건을 화면에 명확히 표시한다.
7. 잘 팔리는 위탁상품은 기존 상품·등록 연결을 유지한 채 성장상품관리에서 더 세밀하게
   관리한다. 소싱 조사 수준의 키워드 근거 분석·상품명·태그 편집과 변경 이력을 지원한다.
8. 사입상품은 소싱 조사에서 고객 니즈·리뷰·상품군 키워드를 깊이 조사한 다음 상품등록관리로
   넘긴다. 상품등록관리에서 목록 이미지·상세페이지·카테고리·속성 등을 완성해 등록한다.

주요 작업 경로는 `위탁상품 가져오기 → 위탁상품관리 → 스마트스토어 → 성장상품관리`와
`소싱 조사 → 상품등록관리 → 스마트스토어`다. 두 경로의 화면 목적은 구분하되 등록,
이미지, 카테고리, 태그 검증과 외부 API 호출은 공통 서비스를 재사용한다.
도매사이트 메모·설정은 보조 도구로 배치한다. 기존 저장 자료는 기능 정리 과정에서 삭제하지 않는다.

공급처 수집 성공과 스마트스토어 반영 성공은 별도 상태로 기록한다. 조회 실패·로그인 필요·
목록에서 찾지 못했다는 사실만으로 단종을 확정하지 않는다. 가격 반영은 사용자가 선택한
마진 정책을 적용한다. 사용자는 공급가 변경 시 기존 마진율을 유지해 판매가도 변경하는
방식을 선택했다. 자동 실행은 기본으로
켜지 않으며, 사용자가 정한 공급처·주기·반영 항목에만 적용한다.

공급처 변경 자동 반영은 가격·품절·명시된 단종·편집하지 않은 상세페이지를 항목별로
선택한다. 품절은 품절 상태, 단종은 판매 중지로 반영하며 판매 재개는 사용자가 검토한다.
상세페이지는 로컬 편집값과 스마트스토어 현재 값이 이전 공급처 원본과 같은 경우에만
교체한다. 새 원본이 비어 있거나 판매자가 가공한 경우에는 수동 검토 경로를 사용한다.
예약 해제·반영 항목 해제는 다음 상품 처리부터 적용한다.

기술 개선은 반복 클릭 감소, 대량 작업 복구, 불필요한 요청·클라이언트 코드 감소를 우선한다.
도매처 신규·변경 확인은 선택한 도매처를 한 번의 실행으로 순차 처리한다. 마지막 선택을
기억하고 도매처별 진행·실패를 구분한다. 도매처 로그인 정보는 확장 프로그램 전용 설정에서
입력하고 PC에 암호화해 저장한다. Chrome 세션에서 보관함을 해제한 뒤 자동 로그인에 쓰며,
CAPTCHA·추가 인증·로그인 실패는 사용자가 해결한다. 비밀번호는 Shoppingday 서버로 보내지 않는다.
반복 작업을 줄이는 구현 선택은 스스로 개선하되 가격 정책·비용·외부 계정 등 중요한 선택은
사용자와 확인한다.
유료 인프라는 실제 사용량·장애·운영 시간 대비 비용을 비교해 추천하고 가입은 별도로 결정한다.

서비스는 다음 두 가지 운영 단계를 명확히 지원해야 한다.

상품 등록 전의 **소싱 조사**는 위 두 단계에 들어가기 전의 의사결정 보조 흐름이다.
키워드 시장, 고객 불편, 리뷰와 품목 위험을 기록해 제한된 상품 슬롯에 넣을 후보를
더 신중하게 고른다. 사용자가 직접 확인한 사실과 판단을 보존하되 특정 체크
조건만으로 재고 소진, 노출 또는 매출을 보장하지 않는다.

### 1. 빠른 등록 단계

- 처음 약 1,000개의 상품을 대량등록 방식으로 빠르게 확보한다.
- 아이템스카우트 엑셀과 규칙 기반 가공을 우선해 상품명·키워드·태그를 빠르게 준비한다.
  AI는 추가 비용 대비 효과가 있는 선택 작업에만 사용한다.
- 개별 상품에 과도한 분석 시간이나 AI 비용을 사용하지 않는다.
- 완벽한 최적화보다 처리 속도와 안정적인 기본 품질을 우선한다.

### 2. 성장 관리 단계

- 등록 후 실제 판매가 발생하거나 판매 가능성이 보이는 상품만 선별한다.
- 선별한 상품에 키워드 순위 추적, 키워드 변경, 상품명 수정 등의 관리 비용을
  집중한다.
- 키워드 및 상품명 변경 이력을 보존한다.
- 매출과 순위 변화를 함께 비교할 수 있도록 발전시킨다.
- 제한된 상품 슬롯 안에서 성장 상품의 매출을 최대화한다.

## 제품 판단 원칙

모든 기능은 아래 질문을 기준으로 설계한다.

> 이 기능이 1,000개의 제한된 상품 슬롯을 더 효율적으로 운영하고,
> 판매 가능성이 있는 상품의 매출을 높이는 데 도움이 되는가?

이 질문에 직접 도움이 되지 않는 복잡한 기능은 초기 MVP에서 제외한다.

추가 원칙은 다음과 같다.

- 빠른 등록과 성장 관리는 목적과 비용 구조가 다르므로 별도 흐름으로 설계한다.
- 모든 상품에 고비용 AI 분석이나 순위 추적을 자동 적용하지 않는다.
- 판매 반응이 확인된 상품에만 추가 비용과 운영 시간을 집중할 수 있어야 한다.
- AI 추천은 사용자의 판단을 돕는 도구이며, 사실 데이터나 최종 결정을 대신하지 않는다.
- 검색량, 순위, 매출 등은 신뢰할 수 있는 실제 데이터 출처를 사용하고 추정값과
  명확히 구분한다.
- 자동화는 상품 슬롯 효율과 매출 개선 효과가 명확할 때 단계적으로 추가한다.

## 현재 기능 설계에의 적용

- 소싱 조사 기능은 등록 전 후보의 키워드 시장·리뷰·품목 위험 근거를 한곳에 기록하는
  경량 조사 도구다. 검색수와 매출은 사용자가 확인한 값을 입력하며 추정값을 자동
  생성하지 않는다.
- 임시저장한 소싱 아이템은 소싱 조사 목록에만 남는다. 사용자가 등록 초안 생성을
  확정한 항목만 상품 등록관리로 이어지고 이때 소싱 상태를 `소싱 결정`으로 바꾼다.
  등록 준비를 시작하면
  네이버 발행에 필요한 기존 상품 초안 저장 구조를 내부적으로 재사용하되, 소싱 등록
  초안은 일반 상품관리 목록과 편집 흐름에 노출하지 않는다. 사용자는 상품 등록관리의
  전용 편집 페이지에서 상품명·태그·가격, 카테고리·속성·재고·이미지·판매 정책을
  확인한 뒤 발행한다.
- 소싱 등록 초안과 공급사에서 가져온 일반 상품은 화면과 운영 목적을 분리하지만,
  네이버 발행 데이터를 별도 모델에 중복 저장하지 않는다.
- 등록 전용 편집에서 사용자가 상품명·태그·가격을 저장한 뒤에는 그 확정값을 우선한다.
  이후 소싱 조사 메모를 수정해도 등록 편집값을 자동으로 덮어쓰지 않는다.
- 소싱 조사에서 특정 직감 상품을 타겟으로 정한 경우 등록 전용 편집에서 이미 수집된
  직감 상품을 상품명·상품번호·상세 URL로 찾아 이미지·상세페이지·옵션·공급가를
  사용할 수 있다. 이때 같은 상품을 복제하지 않고 기존 직감 상품 하나에 소싱 등록을
  연결하며, 직감 원본 콘텐츠와 소싱에서 정한 상품명·검색 태그·판매가·카테고리를
  함께 유지한다.
- 소싱 등록 상품명은 월 검색수 100 이상 관련 후보 중 전체 상위 3개와 검색수 100~1,000
  추가 후보를 자동 연결 후보와 자동 연결 확인 필요 후보로 나눠 보여준다. 사용자는 추천에 사용할 키워드를 여러 개 직접
  선택할 수 있으며, 문자열 자동 연결에 실패했더라도 실제 같은 상품임을 확인한
  후보는 상품명 재료로 사용할 수 있다. 서로 다른 상품형 표현을 50자 안에서 함께
  검토하고 40자 초과부터 즉시 안내하며, 추천 후 실제 반영된 키워드를 보여준다.
- 소싱 등록 검색 태그는 실제 월 검색수를 함께 보여주고 스마트스토어 제한에 맞춰
  최대 10개까지만 선택·저장한다. 월 검색수는 선택 제한으로 사용하지 않으며 상품명과
  중복되는 태그도 기본 선택에서는 제외하되 후보가 부족하면 사용자가 경고 후 직접
  선택할 수 있다.
- 연관 키워드 분류에서 태그 후보는 네이버 추천 태그사전의 정확 일치 코드와 표준 표기를
  저장한다. 속성 후보는 소싱 기준어로 추천한 예상 네이버 카테고리의 공식 속성명·속성값에
  연결하며, 등록 편집의 최종 카테고리 ID가 같을 때만 빈 속성에 자동 입력한다. 사용자가
  이미 선택한 속성값은 자동 연결로 덮어쓰지 않는다.
- 연관 키워드는 상품에 없는 기능·소재·용도 후보를 사용자가 먼저 삭제한 뒤 자동 분류한다.
  자동 분류 전에 실제 판매할 네이버 최종 카테고리를 추천 또는 직접 검색으로 선택하고,
  이 값을 검색 상품군 일치 판정·공식 속성 조회·등록 초안 카테고리에 공통 사용한다.
  검색 의도가 기준 상품과 어긋나더라도 선택 카테고리 공식 속성값이나 추천 태그사전 정확
  일치가 있으면 해당 구조화 위치로 분류하고, 둘 다 없을 때만 삭제 검토로 남긴다. 그 밖에는 `1페이지 자연검색
  상품명의 설정 비율 이상 노출 → 선택 카테고리 공식 속성값 → 네이버 추천 태그사전 정확
  일치 → 카테고리 노출 → 나머지 상품명 후보` 순으로 분류한다. 상품 카드 부가정보 노출만으로
  공식 속성이라고 단정하지 않는다.
- 분석 결과는 카테고리 적합 건수, 상품명 노출 건수, 네이버 태그사전 등록 여부, 선택
  카테고리 공식 속성 연결 여부를 키워드별 상태 카드로 보여준다. 선택 카테고리 일치가
  20% 미만인 키워드는 주로 노출되는 다른 카테고리와 건수를 함께 보여준다. 공식 속성·공식
  태그 근거가 없는 경우에만 삭제 검토 후보로 강조하고 한 번에 모아볼 수 있게 하되 자동으로
  삭제하지 않고 사용자가 표본을 확인해 결정한다.
- 자동 분류의 노출 건수, 카테고리 적합도, 공식 태그·속성 판정, 추천 근거와 상품명 판독
  표본은 키워드 분류와 함께 소싱 아이템에 저장해 다시 열어도 확인할 수 있게 한다. 현재
  분류와 저장된 상세 근거는 엑셀로 내려받아 스마트스토어 상품명·검색 태그를 수동으로
  검토하고 수정하는 작업 자료로 사용할 수 있게 한다.
- 전체 자동 분류에서 상품명 노출 기준을 충족한 키워드는 개수 제한 없이 모두 상품명
  후보로 유지한다. 실제 상품명 생성 단계에서만 월 검색수 상위 3개를 먼저 검토하고,
  나머지 월 검색수 100 이상 1,000 이하 후보를 최대 7개 추가해 조합 검토 대상을 최대
  10개로 제한한다. 상품명 후보에 저장된 추천 태그사전 정확 일치 정보는 별도 태그 풀로
  유지하며, 실제 상품명에 사용되지 않은 공식 태그를 월 검색수순 최대 10개 등록한다.
  정확 일치 공식 태그가 10개보다 적으면 임의의 미등록 태그로 채우지 않는다.
- 소싱 등록과 일반 위탁상품 편집은 같은 검색 최적화 패널을 사용한다. 기본 화면에는
  상품명 키워드·검색 태그·확인 필요 후보의 요약만 표시하고, 사용자가 `키워드 편집`을
  요청했을 때만 전체 후보와 추천 기준을 오버레이로 연다. 소싱 등록은 조사에서 분류한
  후보를 재사용하고 일반 위탁상품은 현재 상품명·공급처 원본명·카테고리를 기준으로
  감지한 상품형과 사용자가 요청한 네이버 키워드 조회 결과를 사용한다.
- 일반 위탁상품 편집에서는 아이템스카우트 엑셀의 실제 키워드와 월 검색수를 가져와
  상품명 키워드 또는 검색 태그로만 빠르게 분류한다. 가져온 후보와 분류 상태는 상품에
  저장해 다시 엑셀을 읽는 중복 작업을 피하고, 삭제·재분류·분류 키워드 기반 상품명
  생성을 같은 작업공간에서 처리한다.
- 상품 이미지는 스마트스토어 규격에 맞춰 대표 이미지 1개와 추가 이미지 최대 9개,
  총 10개까지 저장하며 등록 전에 드래그로 노출 순서를 정리할 수 있다.
- 상품명 조합은 월 검색수 상위 3개를 우선 확보하고 나머지 후보를 5~7개 더 검토한다.
  후보 목록 전체는 보존하되 실제 조합 대상은 대략 8~10개로 제한한다. 같은 의미의 키워드는 사용자
  선택, 기본 상품 유형과의 연결, 실제 검색수, 구체성 순으로 하나를 고르고 서로 다른
  상품 특징을 우선 조합한다. 관련 후보 전체에서 최우선 키워드를 유지하면서 50자를
  넘지 않는 조합 중 제목 길이를 충분히 활용해 제한된 상품 슬롯의 제목이
  반복적이거나 읽기 어려워지는 것을 막는다.
- 기존 상품 등록 기능은 **빠른 등록 단계**에 해당한다.
- 스마트스토어 링크로 상품을 추가하고 키워드·상품명·변경 이력을 관리하는 기능은
  **성장 관리 단계**에 해당한다.
- 성장 상품에서는 사용자가 같은 검색 조건에서 확인한 실제 키워드 순위를 날짜별로
  기록한다. 자동 추정 순위를 만들지 않고 상품명·태그 변경 전후를 이력으로 비교한다.
- 성장 상품의 판매가·재고·판매 상태·상품명·검색 태그·네이버 공식 카테고리 속성을
  한 화면에서 수정하고 최종 확인 후 기존 스마트스토어 상품에 반영한다.
- 위탁 공급처의 API가 없을 때는 성장상품에 저장한 공급처 URL만 사용자가 요청한
  Chrome 점검 대상으로 삼는다. 상품별 확인과 공급처 전체 순차 확인을 제공하되,
  확인 결과만으로 스마트스토어 판매 상태를 자동 변경하지 않는다.
- 최근 7일·30일 판매량은 네이버 주문 API의 결제일과 취소·반품 후 잔여수량을 기준으로
  사용자가 새로고침할 때 집계한다. 조회하지 않은 값을 0이나 추정값으로 표시하지
  않는다.
- 초기 키워드 관리 MVP는 상품 연결, 규칙 기반 상품 분석, 네이버 연관 키워드와 실제
  검색량 확인, 사용자 키워드 선택, 상품명 초안 편집 및 결과 저장에 집중한다.
- 성장 상품 키워드 관리는 외부 생성형 AI 선택 경로 없이 규칙 기반 기본 모드로
  동작한다. 검색량과 경쟁도는 네이버 검색광고 API의 실제 응답만 사용한다.
- 성장상품의 정밀 조사는 소싱의 분류·공식 태그·속성·리뷰 분석 화면을 공통 사용하되,
  조사 결과는 기존 성장상품에 저장하며 별도 소싱 등록을 생성하지 않는다. 저장된 분석은
  상품명·태그 초안으로 미리보고 적용한다. 기존 공식 속성은 보존한다.
- 사용자가 확인해 생성한 대량 등록 작업은 서버 실행을 선택해 화면 종료 후에도 이어갈 수
  있다. 진행·실패 상태를 저장하고 실패 상품만 다시 처리한다. PC 릴레이 가동 조건과 실행
  시간 제한을 명시하며, 예약 생성이나 실제 상품 쓰기를 검증 목적으로 임의 실행하지 않는다.
- 성장 상품의 인기도를 검토할 때는 스마트스토어 공식 SEO 가이드(2025년 3월판)가
  설명하는 최근 1개월의 클릭·리뷰·판매·최신성 범위를 우선 비교하되 실제로 확보한
  값만 사용한다.
  인기도를 다시 얻기 위한 상품 삭제·재등록 반복은 운영 전략으로 사용하지 않는다.

## 문서 유지 원칙

제품의 핵심 운영 전략이 바뀌면 이 문서를 먼저 갱신하고, 그 변화가 라우팅, 데이터
모델, 비용 정책, 외부 API 사용 범위에 미치는 영향을 함께 검토한다. 세부 구현 문서는
이 문서와 충돌해서는 안 된다.
